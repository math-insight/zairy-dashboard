import { Icon } from "leaflet";
import blueIcon from "../../assets/blueIcon.png"

const meteoSensorIcon = new Icon( {
    iconUrl: blueIcon,
    iconSize: [ 32, 32 ]
} )

export default meteoSensorIcon;
