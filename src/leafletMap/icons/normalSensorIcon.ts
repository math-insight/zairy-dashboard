import { Icon } from "leaflet";
import greenIcon from "../../assets/greenIcon.png"

const normalSensorIcon = new Icon( {
    iconUrl: greenIcon,
    iconSize: [ 32, 32 ]
} )

export default normalSensorIcon;
