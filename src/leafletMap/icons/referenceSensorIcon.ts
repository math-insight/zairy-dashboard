import { Icon } from "leaflet";
import yellowIcon from "../../assets/yellowIcon.png"

const referenceSensorIcon = new Icon( {
    iconUrl: yellowIcon,
    iconSize: [ 32, 32 ]
} )

export default referenceSensorIcon;
